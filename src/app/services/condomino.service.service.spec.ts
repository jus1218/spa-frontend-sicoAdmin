
/**


import { TestBed } from '@angular/core/testing';

import { Condomino.ServiceService } from './condomino.service.service';

describe('Condomino.ServiceService', () => {
  let service: Condomino.ServiceService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(Condomino.ServiceService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});



*/
